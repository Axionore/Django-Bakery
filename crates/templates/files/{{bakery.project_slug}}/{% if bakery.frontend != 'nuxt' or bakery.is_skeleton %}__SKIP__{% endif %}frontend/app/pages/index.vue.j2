<script setup lang="ts">
const projectName = useRuntimeConfig().public.appName;
const description = "{{ bakery.description }}";
const auth = useAuthStore();
const { user } = storeToRefs(auth);

useSeoMeta({
  title: () => projectName,
  description: () => description,
});
</script>

<template>
  <section class="space-y-8 py-6">
    <header class="space-y-4">
      <span class="inline-flex items-center gap-1.5 rounded-full bg-emerald-50 px-2.5 py-0.5 text-xs font-medium text-emerald-700 ring-1 ring-inset ring-emerald-600/20 dark:bg-emerald-950 dark:text-emerald-300 dark:ring-emerald-400/20">
        <span class="h-1.5 w-1.5 rounded-full bg-emerald-500"></span>
        Nuxt 4 SSR · OWASP-baselined · Production-ready
      </span>
      <h1 class="text-4xl font-bold tracking-tight">{{ '{{' }} projectName {{ '}}' }}</h1>
      <p class="max-w-2xl text-lg text-slate-600 dark:text-slate-400">{{ '{{' }} description {{ '}}' }}</p>
    </header>
    <div class="flex flex-wrap gap-3">
      <NuxtLink
        :to="user ? '/account/profile' : '/account/login'"
        class="rounded-md bg-slate-900 px-4 py-2 text-sm font-medium text-white hover:bg-slate-700 dark:bg-slate-100 dark:text-slate-900"
      >
        {{ '{{' }} user ? "Your profile →" : "Sign in →" {{ '}}' }}
      </NuxtLink>
{% if bakery.api_layer != 'none' -%}
      <a
        href="/api/docs"
        target="_blank"
        rel="noreferrer"
        class="rounded-md border border-slate-300 px-4 py-2 text-sm font-medium hover:bg-slate-100 dark:border-slate-700 dark:hover:bg-slate-800"
      >
        🚀 API docs
      </a>
{%- endif %}
      <NuxtLink
        to="/about"
        class="rounded-md px-4 py-2 text-sm font-medium hover:bg-slate-100 dark:hover:bg-slate-800"
      >
        Learn more
      </NuxtLink>
    </div>
    <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
      <div class="rounded-xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900">
        <div class="flex items-center justify-between">
          <span class="text-xs font-medium uppercase tracking-wider text-slate-500 dark:text-slate-400">Backend</span>
          <span class="h-2 w-2 rounded-full bg-emerald-500"></span>
        </div>
        <div class="mt-2 text-base font-semibold">Django {{ bakery.django_version }}</div>
        <div class="text-sm text-slate-500 dark:text-slate-400">Python {{ bakery.python_version }}</div>
      </div>
      <div class="rounded-xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900">
        <div class="flex items-center justify-between">
          <span class="text-xs font-medium uppercase tracking-wider text-slate-500 dark:text-slate-400">Auth</span>
          <span class="h-2 w-2 rounded-full bg-emerald-500"></span>
        </div>
        <div class="mt-2 text-base font-semibold">django-allauth</div>
        <div class="text-sm text-slate-500 dark:text-slate-400">MFA TOTP · Argon2id</div>
      </div>
      <div class="rounded-xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900">
        <div class="flex items-center justify-between">
          <span class="text-xs font-medium uppercase tracking-wider text-slate-500 dark:text-slate-400">API</span>
{%- if bakery.api_layer == 'none' %}
          <span class="h-2 w-2 rounded-full bg-slate-300 dark:bg-slate-600"></span>
{%- else %}
          <span class="h-2 w-2 rounded-full bg-emerald-500"></span>
{%- endif %}
        </div>
        <div class="mt-2 text-base font-semibold">{% if bakery.api_layer == 'ninja' %}Django Ninja{% elif bakery.api_layer == 'drf' %}DRF + Spectacular{% elif bakery.api_layer == 'graphql-strawberry' %}Strawberry GraphQL{% elif bakery.api_layer == 'graphql-graphene' %}Graphene GraphQL{% else %}—{% endif %}</div>
        <div class="text-sm text-slate-500 dark:text-slate-400">{% if bakery.api_layer == 'none' %}No API layer{% else %}OpenAPI 3.1 · session-auth{% endif %}</div>
      </div>
      <div class="rounded-xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900">
        <div class="flex items-center justify-between">
          <span class="text-xs font-medium uppercase tracking-wider text-slate-500 dark:text-slate-400">Database</span>
          <span class="h-2 w-2 rounded-full bg-emerald-500"></span>
        </div>
        <div class="mt-2 text-base font-semibold">{% if bakery.relational_db == 'postgres' %}PostgreSQL 18{% elif bakery.relational_db == 'mysql' %}MySQL 9{% elif bakery.relational_db == 'mariadb' %}MariaDB LTS{% else %}SQLite{% endif %}</div>
        <div class="text-sm text-slate-500 dark:text-slate-400">{% if bakery.relational_db == 'sqlite' %}Local-file dev DB{% else %}ATOMIC_REQUESTS · CONN_MAX_AGE{% endif %}</div>
      </div>
    </div>
  </section>
</template>
