<script setup lang="ts">
const description = "{{ cookiecutter.description }}";
useSeoMeta({ title: "About", description: () => description });

const rows: Array<[string, string]> = [
  ["Backend", "Django {{ cookiecutter.django_version }} · Python {{ cookiecutter.python_version }}"],
  ["Frontend", "Nuxt 4 · Vue 3.6 · TypeScript 6 · Tailwind v4"],
{% if cookiecutter.api_layer != 'none' -%}
  ["API", "{{ cookiecutter.api_layer }}"],
{%- endif %}
  ["Auth", "django-allauth headless · MFA TOTP · Argon2id · session cookies"],
];
</script>

<template>
  <article class="max-w-2xl space-y-5 py-6">
    <h1 class="text-3xl font-semibold">About</h1>
    <p class="text-slate-600 dark:text-slate-400">{{ '{{' }} description {{ '}}' }}</p>
    <table class="w-full overflow-hidden rounded-lg border border-slate-200 text-sm dark:border-slate-800">
      <tbody>
        <tr v-for="[label, value] in rows" :key="label" class="border-b border-slate-200 last:border-0 dark:border-slate-800">
          <th scope="row" class="bg-slate-50 px-4 py-3 text-left font-medium dark:bg-slate-900">{{ '{{' }} label {{ '}}' }}</th>
          <td class="px-4 py-3 text-slate-700 dark:text-slate-300">{{ '{{' }} value {{ '}}' }}</td>
        </tr>
      </tbody>
    </table>
  </article>
</template>
