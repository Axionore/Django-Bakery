<script setup lang="ts">
const name = "{{ cookiecutter.project_name }}";
const description = "{{ cookiecutter.description }}";
</script>

<template>
  <main>
    <h1>{{ '{{' }} name {{ '}}' }}</h1>
    <p>{{ '{{' }} description {{ '}}' }}</p>
    <p>
      This is a <strong>skeleton</strong> Vue + Vite scaffold. The Django backend is
      reachable through Vite's dev proxy at <code>/api</code> and <code>/_allauth</code>.
      See <code>frontend/README.md</code> for the security baseline.
    </p>
  </main>
</template>

<style>
main {
  font-family: system-ui, sans-serif;
  padding: 2rem;
  max-width: 640px;
}
</style>
